# Movimiento y anclajes

El movimiento acepta únicamente `none`, `walk`, `teleport` y `return_home`. Los destinos deben existir en `LIAM_ANCHORS`. En móvil no se camina. La caminata requiere bandera, escritorio y ausencia de movimiento reducido. El modo inicial recomendado es teletransporte.

