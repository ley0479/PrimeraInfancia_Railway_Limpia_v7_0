# Zonas seguras

El motor verifica viewport y calcula posiciones laterales o inferiores. El personaje visual no recibe clics; únicamente el panel y sus controles usan `pointer-events`. Antes de ampliar la caminata deben agregarse pruebas de colisión con tablas, formularios, modales y zoom.

